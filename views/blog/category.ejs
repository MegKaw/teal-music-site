<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= category.toUpperCase() %> COLUMN | TEAL MUSIC SCHOOL</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/blog.css" />
  <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="blog-category-page">
    <h1><%= category.toUpperCase() %> COLUMN</h1>
    <p class="category-description">
      <%= category %> に関するコラムをご紹介します。
    </p>

    <section class="blog-card-list">
      <% articles.forEach(article => { %>
        <article class="blog-card">
          <img src="<%= article.image %>" alt="<%= article.title %>">
          <div class="blog-card-content">
            <h2><%= article.title %></h2>
            <p><%= article.summary || '記事の内容をご覧ください。' %></p>
            <a href="/blog/<%= article.category %>/<%= article.slug %>">続きを読む →</a>
          </div>
        </article>
      <% }) %>
    </section>
  </main>

  <%- include('../partials/footer') %>
  <%- include('../partials/floating-cta') %>
  <script src="/js/main.js"></script>
</body>
</html>
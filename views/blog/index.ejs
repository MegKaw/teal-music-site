<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>コラム一覧｜TEAL MUSIC SCHOOL</title>
  <meta name="description" content="ティールミュージックスクールの講師による音楽コラム一覧。ギター・ボーカル・DTM・ベースなど、上達のヒントや練習法を紹介します。">
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    body {
      max-width: 1100px;
      margin: 24px auto;
      font-family: 'M PLUS Rounded 1c', sans-serif;
      padding: 0 12px;
    }
    h1 {
      text-align: center;
      margin-bottom: 32px;
    }
    .post-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 24px;
    }
    .post-card {
      display: block;
      text-decoration: none;
      color: #222;
      border-radius: 10px;
      overflow: hidden;
      border: 1px solid #eee;
      background: #fff;
      transition: box-shadow 0.2s;
    }
    .post-card:hover {
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .post-card img {
      width: 100%;
      height: 160px;
      object-fit: cover;
      border-bottom: 1px solid #eee;
    }
    .post-content {
      padding: 12px 16px;
    }
    .post-content h2 {
      font-size: 1.1rem;
      margin: 0 0 6px;
      line-height: 1.4;
    }
    .post-content p {
      font-size: 0.9rem;
      color: #666;
      margin: 0;
    }
    .category-label {
      display: inline-block;
      background: #0ea5a3;
      color: #fff;
      padding: 3px 8px;
      font-size: 0.8rem;
      border-radius: 4px;
      margin-bottom: 6px;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <main>
    <h1>TEAL MUSIC コラム</h1>

    <% if (posts && posts.length) { %>
      <div class="post-grid">
        <% posts.forEach(p => { %>
          <a href="/blog/<%= p.slug %>" class="post-card">
            <img src="<%= p.coverImage || '/images/blog/placeholder.jpg' %>" alt="<%= p.title %>">
            <div class="post-content">
              <span class="category-label"><%= p.category || 'コラム' %></span>
              <h2><%= p.title %></h2>
              <% if (p.excerpt) { %><p><%= p.excerpt %></p><% } %>
            </div>
          </a>
        <% }) %>
      </div>
    <% } else { %>
      <p>まだ記事はありません。</p>
    <% } %>

    <section class="admin-entry">
      <a href="/admin/login" class="admin-login-link" target="_blank" rel="noopener noreferrer">管理者ログイン</a>
    </section>
  
    <style>
      .admin-entry{margin:36px 0;text-align:center}
      .admin-btn{
        display:inline-block; padding:10px 16px; border-radius:8px;
        background:#0ea5a3; color:#fff; text-decoration:none; font-weight:700;
      }
      .admin-btn:hover{opacity:.9}
    </style>
  </main>

  <%- include('../partials/footer') %>
  <%- include('../partials/floating-cta') %>
  <script src="/js/main.js"></script>
</body>
</html>